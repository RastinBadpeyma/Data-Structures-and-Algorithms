// Rule 3 : Different terms for inputs

function comparessBoxesTwice(boxes: string[] , boxes2: string[]){
   boxes.forEach(function(boxes){
      console.log(boxes);
   })


   boxes2.forEach(function(boxes){
      console.log(boxes);
   });
}

// O(a + b);